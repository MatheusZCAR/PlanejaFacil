import { Locator, WebDriver, WebElement } from 'selenium-webdriver';
export declare class FlutterSeleniumBridge {
    private driver;
    constructor(driver: WebDriver);
    enableAccessibility(timeout?: number): Promise<void>;
    activateInputField(locator: Locator, timeout?: number): Promise<WebElement>;
}
export default FlutterSeleniumBridge;
